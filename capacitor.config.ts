import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'uah-aplicaciones-moviles-hibridas-trabajoInicial',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
